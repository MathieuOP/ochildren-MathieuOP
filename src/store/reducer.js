/**
 * Initial State
 */
const initialState = {
  
};

/**
 * Types
 */

/**
 * Traitements
 */

/**
 * Reducer
 */
const reducer = (state = initialState, action = {}) => {
  switch (action.type) {

    default:
      return state;
  }
};

/**
 * Action Creators
 */

/**
 * Selectors
 */

/**
 * Export
 */
export default reducer;
